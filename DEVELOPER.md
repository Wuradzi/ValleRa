# 📚 ValleRa - Документація для розробників

## Архітектура

```
┌─────────────────────────────────────────┐
│        Голосова команда (User)          │
└──────────────────┬──────────────────────┘
                   │
        ┌──────────▼──────────┐
        │  Listener (Listen)  │ ◄─ Розпізнавання мовлення
        └──────────┬──────────┘
                   │
        ┌──────────▼──────────────────────┐
        │  CommandProcessor (Обробка)     │
        │  - Парсинг текстуu              │
        │  - Маршрутизація команд         │
        └──────────┬──────────────────────┘
                   │
        ┌──────────▼──────────────────────┐
        │     Skills (Навички)            │
        │  ┌──────────────────────────┐   │
        │  │ system_skills.py         │   │
        │  │ program_skills.py        │   │
        │  │ search_skills.py         │   │
        │  │ note_skills.py           │   │
        │  │ media_skills.py          │   │
        │  │ utility_skills.py        │   │
        │  │ translation_skills.py    │   │
        │  └──────────────────────────┘   │
        └──────────┬──────────────────────┘
                   │
        ┌──────────▼───────────┐
        │  VoiceEngine (Speak) │ ◄─ Синтез мовлення (TTS)
        └──────────────────────┘
                   │
        ┌──────────▼──────────────────┐
        │    Динамік (Output)         │
        └─────────────────────────────┘
```

## Модульна система навичок

### Структура модуля

Кожний файл навичок (`skills/*.py`) містить:

```python
"""Опис категорії навичок"""

def skill_function(text, voice=None, listener=None):
    """
    Стандартна сигнатура навички
    
    Args:
        text (str): Текст команди від користувача
        voice (VoiceEngine): Об'єкт для озвучення (опціонально)
        listener (Listener): Об'єкт для розпізнавання мовлення (опціонально)
    
    Returns:
        str: Текст для озвучення користувачу
    """
    result = do_something(text)
    return f"Результат: {result}"
```

### Експорт навичок

Всі навички мають експортуватись з `skills/__init__.py`:

```python
from .system_skills import *
from .program_skills import *
# ...

__all__ = [
    'get_time',
    'get_date',
    'open_program',
    # ...
]
```

## Вбудовані навички

### system_skills.py
```python
turn_off_pc(text=None)              # Вимкнення комп'ютера
cancel_shutdown(text=None)          # Скасування вимкнення
lock_screen(text=None)              # Блокування екрану
wake_up_pc(text=None)               # Пробудження комп'ютера
system_status(text=None)            # Статус системи (CPU, RAM)
```

### program_skills.py
```python
open_program(text, voice, listener) # Запуск програми
close_app(text, voice, listener)    # Закриття програми
is_app_name(text)                   # Перевірка чи це назва програми
list_processes(text, voice, listener) # Список процесів
```

### search_skills.py
```python
search_internet(text)               # Пошук в DuckDuckGo
check_weather(text)                 # Поточна погода
weather_forecast(text, voice, listener) # Прогноз погоди
search_google(t)                    # Пошук на Google
search_youtube_clip(t)              # Пошук на YouTube
```

### note_skills.py
```python
add_note(text, voice, listener)     # Додати нотатку
show_notes(text, voice, listener)   # Показати нотатки
clear_notes(text, voice, listener)  # Очистити нотатки
```

### media_skills.py
```python
volume_up(text=None)                # Збільшити гучність
volume_down(text=None)              # Зменшити гучність
media_play_pause(text=None)         # Грай/пауза
media_next(text=None)               # Наступна дорівка
media_prev(text=None)               # Попередня дорівка
take_screenshot(text=None)          # Скріншот екрану
look_at_screen()                    # Скріншот для AI
read_clipboard(text, voice, listener) # Читати буфер обміну
```

### utility_skills.py
```python
get_time(text=None)                 # Час
get_date(text=None)                 # Дата
timer(text, voice, listener)        # Таймер
check_timers()                      # Перевірити таймери
calculator(text, voice, listener)   # Калькулятор
remember_data(text, voice, listener) # Запам'ятати дані
recall_data(text, voice, listener)  # Згадати дані
get_help(text, voice, listener)     # Допомога
```

### translation_skills.py
```python
translate_text(text, voice, listener) # Переклад
show_translations(text, voice, listener) # Історія перекладів
```

## Додавання нової навички

### Крок 1: Створи файл навички

```python
# skills/weather_skills.py
"""
Weather Skills - Розширена робота з погодою
"""

def get_sunrise_sunset(city):
    """Отримує час сходу та заходу сонця"""
    # Твоя реалізація
    return f"Сходу в 6:30, заходу в 18:45"

def weather_alert(city):
    """Перевіряє алерти про погоду"""
    # Твоя реалізація
    return "Немає алертів"
```

### Крок 2: Експортуй в __init__.py

```python
# skills/__init__.py
from .weather_skills import *

__all__ = [
    # ... попередні
    'get_sunrise_sunset',
    'weather_alert',
]
```

### Крок 3: Додай команду в processor.py

```python
# core/processor.py
self.hard_commands = {
    # ... попередні
    ("сонце", "схід", "захід"): skills.get_sunrise_sunset,
    ("алерт", "застереження"): skills.weather_alert,
}
```

## Файли даних

### Пам'ять `~/.valera_memory.json`
```json
{
  "ключ1": "значення1",
  "улюблений фільм": "Інтерстеллар",
  "день народження": "1 січня"
}
```

### Нотатки `~/.valera_notes.txt`
```
[2026-02-26 14:30] Купити хліб
[2026-02-26 15:45] Позвонити мамі
```

### Переклади `~/.valera_translations.json`
```json
{
  "hello...": {
    "original": "hello world",
    "translated": "привіт світ",
    "lang": "uk"
  }
}
```

## Очистка кешу аудіо

Кеш автоматично очищується при завершенні:

```python
# main.py
def cleanup_audio_cache():
    """Очищує кеш аудіо файлів після завершення роботи."""
    cache_dir = "audio_cache"
    if os.path.exists(cache_dir):
        shutil.rmtree(cache_dir)
        logger.info(f"✅ Кеш аудіо очищено")
```

### Ручна очистка

```bash
# Видалити папку кешу
rm -rf audio_cache

# Або видалити окремі файли
rm audio_cache/*.wav
```

## Конфігурація

### config.py

```python
# API ключи
OPENAI_API_KEY = "sk-..."
GOOGLE_API_KEY = "AIza..."

# Голос
VOICE = "uk"  # Мова
VOICE_SPEED = 1.0  # Швидкість

# Активація
TRIGGER_WORDS = ["валєра", "привіт"]

# Таймаути
LISTEN_TIMEOUT = 10  # секунд
```

### .env

```
OPENAI_API_KEY=sk-...
GOOGLE_API_KEY=AIza...
ELEVENLABS_API_KEY=...
```

## Тестування

### Текстовий режим

```bash
python main_text.py
```

```
👤 Ти: який час?
⚡ Обробка: який час?
🔊 Голос: Зараз 14:30

👤 Ти: порахуй 2*2
⚡ Обробка: порахуй 2*2
🔊 Голос: 2*2 = 4
```

### Unit-тести

```python
# tests/test_skills.py
import unittest
from skills import calculator, get_time

class TestSkills(unittest.TestCase):
    def test_calculator(self):
        result = calculator("порахуй 2+2")
        self.assertIn("4", result)
    
    def test_get_time(self):
        result = get_time()
        self.assertRegex(result, r'\d{2}:\d{2}')
```

Запуск тестів:
```bash
python -m pytest tests/
```

## Залежності та вимоги

```txt
# requirements.txt
SpeechRecognition==3.10.0
pyttsx3==2.90
pydub==0.25.1
requests==2.31.0
thefuzz==0.19.0
python-Levenshtein==0.21.1
pyperclip==1.8.2
psutil==5.9.6
duckduckgo-search==3.9.2
googletrans==4.0.47
pyautogui==0.9.53
python-dotenv==1.0.0
colorama==0.4.6
```

## Отримання допомоги

- 📖 Документація: див. README.md
- 🐛 Баги: відкрий Issue на GitHub
- 💬 Обговорення: GitHub Discussions

## Контрибючинг

1. Fork репозиторій
2. Cтвори гілку (`git checkout -b feature/AmazingFeature`)
3. Commit змін (`git commit -m 'Add some AmazingFeature'`)
4. Push на гілку (`git push origin feature/AmazingFeature`)
5. Відкрий Pull Request

---

**Останнє оновлення:** Лютий 2026
**Версія:** 2.0.0
