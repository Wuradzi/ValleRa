# ⚡ Швидкий старт

## За 5 хвилин до першої команди

### 1️⃣ Встановлення (2 хвилини)

```bash
# Клонування
git clone https://github.com/Wuradzi/ValleRa.git
cd ValleRa

# Віртуальне середовище
python3 -m venv venv
source venv/bin/activate

# Залежності
pip install -r requirements.txt
```

### 2️⃣ Налаштування (2 хвилини)

```bash
# Копіювання конфігурації
cp .env.example .env

# Вибірково: додай API ключі в .env
# Більшість функцій працюють без них!
```

### 3️⃣ Перший запуск (1 хвилина)

#### 🎤 Голосовий режим
```bash
python main.py
# Скажи "Валєра" + команду
```

#### 💬 Текстовий режим (для тестування)
```bash
python main_text.py
# Введи команду в консоль
```

#### 🔔 Режим гарячого слова (завжди слухає)
```bash
python main_hotword.py
# Асистент активується автоматично при "Валєра"
```

## Перші команди для спробування

```
"Валєра, який час?"        ✅ Робить без інтернету
"Валєра, порахуй 2+2"      ✅ Одразу відповідає
"Валєра, таймер 2 хвилини" ✅ Встановлює таймер
"Валєра, погода"           ✅ Показує погоду (потребує інтернету)
"Валєра, допомога"         ✅ Список всіх команд
```

## Розв'язування проблем

### ❌ "ModuleNotFoundError: No module named 'skills'"

Переконайся, що ти в корневій папці проекту:
```bash
ls skills/  # Повинна бути папка
```

Якщо папки немає - ти скопіював старий код. Онови репозиторій:
```bash
git pull origin main
```

### ❌ "Не чути мовлення"

```bash
# Linux: перевір мікрофон
pactl list short sources
```

### ❌ "Помилка при запуску"

Спробуй текстовий режим без мікрофона:
```bash
python main_text.py
```

## Структура проекту (важливо розуміти)

```
ValleRa/
├── skills/                 ◄─ ВСІ КОМАНДИ (розділені на модулі)
│   ├── system_skills.py    - Вимкнення, блокування, статус
│   ├── program_skills.py   - Запуск програм
│   ├── search_skills.py    - Пошук, погода
│   └── ...                 - Ще навички
│
├── core/                   ◄─ ЯДРО
│   ├── listen.py          - Розпізнавання мовлення (вхід)
│   ├── processor.py        - Маршрутизація команд
│   └── speak.py           - Синтез мовлення (вихід)
│
├── main.py                 ◄─ ЗАПУСК
├── config.py               ◄─ НАЛАШТУВАННЯ
└── requirements.txt        ◄─ ЗАЛЕЖНОСТІ
```

## Налаштування для себе

### Зміна мови/локалі

Відредагуй `config.py`:
```python
VOICE = "uk"  # uk = українська, en = англійська
LANGUAGE = "uk"
```

### Додавання свої команди

1. Додай функцію в `skills/my_skills.py`
2. Експортуй в `skills/__init__.py`
3. Додай правило в `core/processor.py`

Приклад:
```python
# skills/custom_skills.py
def say_hello(text, voice=None, listener=None):
    return "Привіт, світ!"

# skills/__init__.py
from .custom_skills import *
__all__ = [..., 'say_hello']

# core/processor.py
("привіт", "спочатку"): skills.say_hello,
```

## Гарячі клавіші

| Сполучення | Що робить |
|------------|-----------|
| Ctrl+C | Вихід (очищує кеш) |
| Enter (text mode) | Відправити команду |

## Файли, які створюються автоматично

```
~/.valera_memory.json        ← Пам'ять асистента
~/.valera_notes.txt          ← Твої нотатки
~/.valera_translations.json  ← Історія перекладів
audio_cache/                 ← Кеш аудіо (очищується автоматично)
```

## Наступні кроки

📖 **Для розробників:**
- Читай `DEVELOPER.md` для детальної документації
- Досліджуй `skills/*.py` для розуміння структури
- Додавай свої команди в `skills/`

🎯 **Для користувачів:**
- Спробуй усі команди з допомоги (`"Валєра, допомога"`)
- Налаштуй під себе (змінні в `config.py`)
- Додавай нотатки, пам'ять, таймери

🌟 **Для контрибютив:**
- Fork репозиторію
- Створи нові навички
- Скидай Pull Request

---

**Натяк:** Якщо щось не працює, спробуй текстовий режим:
```bash
python main_text.py
```

Він не потребує мікрофона і допомагає тестувати команди.

**Питання?** Відкрий Issue на GitHub: https://github.com/Wuradzi/ValleRa/issues
